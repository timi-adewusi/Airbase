## airbase.nim
## The Airbase language — unified entry point.
## Import this module to use the full Airbase DSL.
##
##   "Nothing is allowed unless it is proven legitimate."
##
## Airbase programs do not "run" — they are allowed to exist.

import airbase_core
import airbase_macros
import airbase_dsl
import airbase_types
import airbase_runtime
import airbase_sim
import airbase_contracts

export airbase_core
export airbase_macros
export airbase_dsl
export airbase_types
export airbase_runtime
export airbase_sim
export airbase_contracts

